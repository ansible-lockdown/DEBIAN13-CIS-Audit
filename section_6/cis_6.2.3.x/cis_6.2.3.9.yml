---

{{ if .Vars.deb13cis_level_2 }}
  {{ if .Vars.deb13cis_rule_6_2_3_9 }}
command:
  auditd_NWmgr_cnf:
    title: 6.2.3.9 | Ensure events that modify /etc/NetworkManager directory are collected | conf check
    exec: |
      if [ -e /etc/NetworkManager ]; then
      grep -hs '^-a .*system-locale' /etc/audit/rules.d/*.rules | grep -qE -- "(path|dir)=/etc/NetworkManager/?([[:space:]]|$)" || echo "MISSING /etc/NetworkManager"
      fi
    exit-status: 0
    stdout:
    - '!/./'
    meta:
      server: 2
      workstation: 2
      CIS_ID:
      - 6.2.3.9
      NIST800-53R5:
      - AU-3
      - CM-6
  auditd_MWmgr_live:
    title: 6.2.3.9 | Ensure events that modify /etc/NetworkManager directory are collected | running
    exec: |
      if [ -e /etc/NetworkManager ]; then
      auditctl -l 2>/dev/null | grep system-locale | grep -qE -- "(path|dir)=/etc/NetworkManager/?([[:space:]]|$)" || echo "MISSING /etc/NetworkManager"
      fi
    exit-status: 0
    stdout:
    - '!/./'
    meta:
      server: 2
      workstation: 2
      CIS_ID:
      - 6.2.3.9
      NIST800-53R5:
      - AU-3
      - CM-6
  {{ end }}
{{ end }}
